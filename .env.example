# YouTube Clipper 环境变量配置示例
# 复制此文件为 .env 并填入实际值

# ============================================================================
# FFmpeg 配置
# ============================================================================

# FFmpeg 可执行文件路径（可选，留空则自动检测）
# macOS (Apple Silicon): /opt/homebrew/opt/ffmpeg-full/bin/ffmpeg
# macOS (Intel): /usr/local/opt/ffmpeg-full/bin/ffmpeg
# Linux: /usr/bin/ffmpeg
FFMPEG_PATH=

# 字幕烧录样式
SUBTITLE_FONT_SIZE=24
SUBTITLE_MARGIN_V=30

# ============================================================================
# 输出配置
# ============================================================================

# 输出目录（默认: skill 目录下的 .output/）
OUTPUT_DIR=./.output

# 视频质量限制（最高分辨率）
# 选项: 720, 1080, 1440, 2160 (4K)
MAX_VIDEO_HEIGHT=1080

# ============================================================================
# 字幕翻译配置
# ============================================================================

# 批量翻译大小（每批翻译的字幕条数）
# 推荐: 20-25
TRANSLATION_BATCH_SIZE=20

# 目标翻译语言
# 选项: 中文, 日语, 韩语, 西班牙语, 等
TARGET_LANGUAGE=中文

# ============================================================================
# 章节分析配置
# ============================================================================

# 目标章节时长（秒）
# 推荐: 180 (3分钟) - 300 (5分钟)
TARGET_CHAPTER_DURATION=180

# ============================================================================
# yt-dlp 配置
# ============================================================================

# yt-dlp 代理设置（如果需要）
# 格式: http://proxy-server:port 或 socks5://proxy-server:port
YT_DLP_PROXY=

# yt-dlp 下载速率限制（如果需要）
# 格式: 50K, 4.2M, 等
YT_DLP_RATE_LIMIT=

# ============================================================================
# 无字幕时：语音转文字（faster-whisper）
# ============================================================================
# 当 YouTube 无人工/自动字幕时，可用 faster-whisper 从音频生成字幕

# 启用无字幕时自动转写（未配置或留空则视为 true，即启用）
# 选项: true, false
WHISPER_ENABLED=true

# 运行方式：local（本地）或 remote（远程 API）
# 默认: local
WHISPER_MODE=local

# 本地模式下的模型大小（仅当 WHISPER_MODE=local 时有效）
# 选项: tiny, base, small, medium, large-v2, large-v3
# 默认: small（速度与质量平衡）
WHISPER_MODEL=small

# 本地模式下的计算设备（仅当 WHISPER_MODE=local 时有效）
# 选项: cpu, cuda, auto
WHISPER_DEVICE=cpu

# 远程模式下的 API 地址（仅当 WHISPER_MODE=remote 时有效）
# 使用 multipart/form-data（audio_file=音频, output_format, language），返回 SRT/VTT 或 JSON
# 推荐服务见 references/whisper-api.md，示例: http://localhost:10888/asr
WHISPER_API_URL=

# 远程 API 请求参数（可选，有默认值）
# 请求的 output_format，默认 srt（便于生成带时间戳字幕）
WHISPER_API_OUTPUT_FORMAT=srt
# 请求的 language，默认 auto
WHISPER_API_LANGUAGE=auto

# ============================================================================
# 高级配置
# ============================================================================

# 临时文件目录（用于解决路径空格问题）
# 留空则使用系统默认临时目录
TEMP_DIR=

# 是否保留临时文件（调试用）
# 选项: true, false
KEEP_TEMP_FILES=false

# 日志级别
# 选项: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# ============================================================================
# 注意事项
# ============================================================================

# 1. 此文件中的配置是可选的，如果不设置，Skill 会使用默认值
# 2. 路径可以使用 ~ 表示用户主目录
# 3. 环境变量值不需要引号
# 4. 以 # 开头的行是注释
# 5. 修改配置后需要重启 Claude Code 才能生效
